@model IEnumerable<ENEKweb.Areas.Admin.Models.TehtudTood.TehtudTooModel>
@{
    ViewData["Title"] = "TEHTUD TÖÖD";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>@ViewData["Title"]</h2>
@await Html.PartialAsync("_StatusMessagePartial", TempData["StatusMessage"])
<div class="container-fluid p-0">
    <div class="row no-gutters">
        <div class="col-md-12">
            <p>
                <a class="btn btn-primary" asp-action="Create">Create New</a>
            </p>
            <div class="table-responsive-md">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Name)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.YearDone)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.BuildingType)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Images)
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var tehtudToo in Model) {
                            <tr class="admin-table-tr">
                                <td>
                                    @Html.DisplayFor(modelItem => tehtudToo.Name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => tehtudToo.YearDone)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => tehtudToo.BuildingType)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => tehtudToo.Images.Count)
                                </td>
                                <td>
                                    <div class="btn-group-sm" role="group" aria-label="Item manipulate options">
                                        <a asp-action="Edit" asp-route-id="@tehtudToo.Id" class="btn btn-primary">Edit</a>
                                        <a asp-action="Details" asp-route-id="@tehtudToo.Id" class="btn btn-secondary">Details</a>
                                        <a asp-action="Delete" asp-route-id="@tehtudToo.Id" class="btn btn-danger">Delete</a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>


